import React from "react";
import DynamicCollectionManager from "../components/DynamicCollectionManager";

const PressReleasesDataManager: React.FC = () => {
  return <DynamicCollectionManager collectionId="about/pressreleasesdata" />;
};

export default PressReleasesDataManager;
