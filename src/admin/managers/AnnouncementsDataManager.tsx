import React from "react";
import DynamicCollectionManager from "../components/DynamicCollectionManager";

const AnnouncementsDataManager: React.FC = () => {
  return <DynamicCollectionManager collectionId="home__announcementsdata" />;
};

export default AnnouncementsDataManager;
